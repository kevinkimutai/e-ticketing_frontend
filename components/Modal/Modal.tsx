import React from "react";

type ComponentProps = {
  title: string;
  description: string;
};

const Modal = ({ title, description }: ComponentProps) => {
  return <div></div>;
};

export default Modal;
